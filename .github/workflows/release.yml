name: Release

# 推送 v* 标签时：用 CI 密钥签名构建三包 APK → 发布到 Releases → 部署 update.json 到 GitHub Pages
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Decode signing keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.SIGNING_KEYSTORE_BASE64 }}
        run: |
          mkdir -p keystore
          printf '%s' "$KEYSTORE_BASE64" | base64 -d > keystore/mdot-release.keystore
          cat > keystore/keystore.properties <<'EOF'
          storeFile=keystore/mdot-release.keystore
          storePassword=${{ secrets.SIGNING_STORE_PASSWORD }}
          keyAlias=${{ secrets.SIGNING_KEY_ALIAS }}
          keyPassword=${{ secrets.SIGNING_KEY_PASSWORD }}
          EOF

      - name: Build release APKs
        run: |
          chmod +x ./gradlew
          ./gradlew :app:assembleRelease --no-daemon --stacktrace

      - name: Rename APKs (v8a / v7a / dual)
        run: |
          VER=$(grep versionName app/build.gradle.kts | sed -E 's/.*"([0-9.]+)".*/\1/' | head -1)
          VC=$(grep versionCode app/build.gradle.kts | grep -oE '[0-9]+' | head -1)
          echo "VER=$VER" >> "$GITHUB_ENV"
          echo "VC=$VC" >> "$GITHUB_ENV"
          OUT=app/build/outputs/apk/release
          cp "$OUT/app-arm64-v8a-release.apk"   "MDOT-RELEASE-${VER}-v8a.apk"
          cp "$OUT/app-armeabi-v7a-release.apk" "MDOT-RELEASE-${VER}-v7a.apk"
          cp "$OUT/app-universal-release.apk"   "MDOT-RELEASE-${VER}-dual.apk"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: 马的加班 v${{ env.VER }}
          body: |
            马的加班 v${{ env.VER }} 自动构建发布。

            下载说明：
            - 大多数现代手机下载 **v8a**（arm64）
            - 老款 32 位设备下载 **v7a**
            - 不确定就下载 **dual**（双架构通用包，体积更大）

            系统要求：Android 10（API 29）及以上。
          files: |
            MDOT-RELEASE-${{ env.VER }}-v8a.apk
            MDOT-RELEASE-${{ env.VER }}-v7a.apk
            MDOT-RELEASE-${{ env.VER }}-dual.apk

      - name: Generate update.json
        run: |
          mkdir -p pages-site
          cat > pages-site/update.json <<'EOF'
          {
            "versionName": "${{ env.VER }}",
            "versionCode": ${{ env.VC }},
            "tag": "v${{ env.VER }}",
            "releaseNotes": "马的加班 v${{ env.VER }} 已发布。",
            "apkUrl": {
              "arm64-v8a": "https://github.com/${{ github.repository }}/releases/download/v${{ env.VER }}/MDOT-RELEASE-${{ env.VER }}-v8a.apk",
              "armeabi-v7a": "https://github.com/${{ github.repository }}/releases/download/v${{ env.VER }}/MDOT-RELEASE-${{ env.VER }}-v7a.apk",
              "universal": "https://github.com/${{ github.repository }}/releases/download/v${{ env.VER }}/MDOT-RELEASE-${{ env.VER }}-dual.apk"
            }
          }
          EOF

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: pages-site

  deploy-pages:
    needs: build-and-release
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
