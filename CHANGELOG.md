# 更新日志（CHANGELOG）

> 本文件记录每个发布版本的用户可读更新说明。
> **发版流程**：发版前在本文件新增 `## vX.Y.Z (YYYY-MM-DD)` 段落 → 同步到 public-main → CI 自动提取该段落作为 GitHub Release 的 body（格式：`## 更新内容` + 变更列表 + 系统要求，不含下载说明）。
> 格式：二级标题为版本号+日期，下方用无序列表列出变更；面向用户，避免内部实现细节。
> 技术细节与完整批次记录见 [docs/00-交接与开发状态.md](docs/00-交接与开发状态.md)。

## v0.7.5（2026-09-23）

**「我的」页：数字都能点了**

- 本年数据三列**全部变成入口**：
  - 上班制度为工地记工时：点「工天」进统计页、点「完工项目」进项目列表、点「工钱」进统计页
  - 其余制度：点「加班」进统计页、点「天数」进日历、点「调休余额」**直达制度设定的「调休」页签**（此前这个余额没有任何入口）
- **个性签名可以改了**：点头像下方的签名即可编辑，先给一批现成短句选（也支持自定义，24 字以内）
- 数据条的数字格子去掉了小箭头，点格子即可跳转

**个人所得税估算页**

- **月份导航可以点月份直接选**（弹出与记月页同一套月份选择器），不在当前月时旁边还有「回本月」
- 页面瘦身：免责声明移到页脚，首屏先出结果，滚动更短

**记月页**

- 实发占比条三段配色改为同色系**深浅分层**（此前基本工资与加班工资两段几乎同色，不好区分）

**修复**

- 个性签名弹窗点「保存」偶尔不生效

## v0.7.4（2026-09-23）

**记月页：从"手填"变成"自动"**

- 新增 **实发工资** 汇总卡：进页面第一眼就看到本月实发（应发 − 扣款 − 其他），不用再把四个分组自己加一遍；实发为负会标红
- 汇总卡顺带给出 **考勤摘要**：本月加班多少小时（平日 / 周末 / 法定分开）、请假多少、记了几天，点一下直接跳到明细
- **加班工资、事假、病假、调休折现、社保、公积金现在都由「同步本月考勤」自动回填**，不需要手工算
  - **调休折现**：你在记加班时勾了「转调休」，这段时间折算多少钱由 App 按你自己的时薪与倍率算，
    行上会写明「本月转调休 1 小时」——不必再自己填天数 × 日薪
  - **社保 / 公积金**：在记月里点「社保」/「公积金」那一行，**弹窗里按当地比例选一次**
    （社保 8% / 10.2% / 10.5%，公积金 5%–12%），缴费基数默认跟随底薪（也可单独填）；之后每月同步自动计入，行上写明「基数 5000 × 10.5%」
  - 没启用的项目（选「不算」）**不会被自动改写**，你手填的金额原样保留
- 每行金额都标 **来源**：「来自考勤 · 9/22 同步」/「已手改（引擎 X）」——哪个数是算出来的、哪个是自己改的，一眼分清
- **对账**：只要某行被改得和考勤计算不一致，汇总卡就会显示「N 项与考勤计算不一致（合计 ±X）」；
  点开可看「引擎 79.31 → 现值 60（-19.31）」，并能**一键恢复**成计算值
- 汇总卡右上给出 **较上月** 的增减（上月没记过则不显示）

**工资单导出：与记月同一份数据**

- 「生成工资单长图」现在**默认按你在记月里维护的那张单**出图（分组明细 + 小计 + 实发）——
  发到微信核对的和 App 里看到的是同一个数（此前长图只按考勤计算，社保 / 公积金 / 个税都不会出现）
- 导出页新增数据源选择：**记月单据**（默认）/ **考勤计算**（旧口径保留）；该月记月还没填时自动回落到考勤计算并提示

**个人所得税估算（新页面）**

- 记月页的「个人所得税（新）」点进去是**独立页面**，按国家 **累计预扣法** 估算本月应预扣税额
- 页面上的数几乎不用你填：累计收入、累计社保 + 公积金、累计已预缴个税都从**今年的记月**自动汇总；
  你只需**勾一次专项附加扣除**（子女教育 / 婴幼儿照护 / 赡养老人 / 继续教育 / 房贷利息 / 住房租金，标准写在选项里）
- 估算结果可以改成实际数，点「填入记月」写回那一行
- ⚠️ **估算仅供参考，实际以单位代扣与年度汇算为准**

**日期选择：四处统一成一套**

- 日期 / 月份选择此前有**四种长得都不一样**的做法（记加班的日历、记工的日期排、记月的月份格…），现在统一成同一套：
  一样的月份导航、一样的「一~日」表头（周一起）、一样的高亮与今天标记
- **点一下日期即生效并关闭**（不再需要再点一次「确定」）；可选日期跨月时，导航行右侧有「回今天 / 回本月」
- 记工的「多选日期」（补记前几天）也换成同一套日历：**能看出星期几、能跨月**，并显示「已选 N 天」，可一键「清空」
- 日期选择统一为「不能选未来」（记工/加班/导出/统计自定义区间一致）
- 记工补记前几天：**点日期行打开选择器后，标题旁给「多选」切换**——切过去点日期即勾选、显示「已选 N 天」，
  完成后写回（最早的一天作主日期）。此前这个入口是行内三个小字（≈32dp 很难点、也没说明），
  行上不再放第二个按钮（会挤压日期显示）

**记月用起来更顺手**

- 月份标题可点，弹出**年月选择器**（跨月核对不必再连点箭头）；不在本月时旁边常驻「回本月」
- 添加补贴 / 扣款 / 基本项 / 其他项：常用项的选项可以**多选一次添加**；四个分组现在都能自己加行
- 分组的展开 / 收起会**记住**，切页签、重启后保持
- 当月有考勤、但记月还是空的时候，汇总卡会提示「先点同步本月考勤，按记录自动回填」

**记月汇总卡：两种形态 + 分组默认收起**

- 汇总卡现在有**两种形态**：**点卡片头部**即可在「完整」与「精简」之间切换——精简形态只留
  「实发 + 考勤 + 应发构成条」，高度约为完整形态的一半，平时扫一眼就够
- 下面四个分组（基本 / 补贴 / 扣款 / 其他）**默认收起**，进页面先看到汇总，点分组标题才展开
- 汇总卡里的考勤摘要改成一排小标签（如「加班 4 小时」「记了 1 天」），点它直接跳明细

**社保 / 公积金的设置挪到了它自己那一行**

- 之前要在「工资设定」页里设社保比例与缴费基数，现在**点记月里的「社保」/「公积金」行**，
  在弹窗里就能设（比例给预设选项、基数默认跟随底薪）——设置在用到它的地方，工资设定页也清爽了

**个人所得税页更好用**

- 页面瘦身：**数据来源默认收起**（点标题展开明细）、**专项附加扣除收进弹窗**，一屏就能看完
- 专项附加扣除按「**常用**」（子女教育 / 婴幼儿照护 / 赡养老人 / 住房贷款利息）与「其他」分组，常用置顶；
  弹窗顶部的**每月合计随勾选实时更新**，一眼看到这次调整能多扣多少
- 应预扣预缴金额**可以直接点着改**（像记工的包工金额那样），不必再另找一个输入框

## v0.7.3（2026-09-22）

**底栏配置页**

- 「更多选项」改名为 **More**，并搬到页面底部：不再套一层卡片，与上方卡片留 12dp 间距，看起来清爽多了
- 每个配置项的图标**补上了底色方块**（与首页卡片配置页同款）：打开时浅橙底、关闭时灰底，一眼就能看出开关状态
- 配置项**长按即可拖动排序**：原来的六点拖拽手柄已去掉，行更干净；四个配置页（底栏 / 首页卡片 / 周期工作日·班次 / 工地项目）统一同一套手势
- **已关闭的配置项现在也能拖动排序**（此前被锁住不能拖）
- 工地项目行：项目信息压成一行不换行，名称不再被挤断；非当前项目的信息变淡，一眼分清哪个在用

**视觉细节**

- 全应用**圆角与间距统一到设计档位**：此前散落的 6dp / 10dp / 14dp 等"半档"间距全部收敛到 4 / 8 / 12 / 16 / 24 档 —— 标签、药丸、列表行、顶栏的内边距因此整齐了（标签类会略微宽松一点）
- **图标尺寸与底色方块统一**：图标尺寸归入几档标准值；设置行、工地卡片、首页入口卡的图标底统一为 36dp 方块，方块圆角只保留 12dp 与 16dp 两档
- 导出页的 Excel 方块、图表气泡、热力图小格、记加班按钮与悬浮按钮的圆角一并归入同一套档位（观感不变，今后调整不会再"各写各的"）
- **说明文字精简**：61 条啰嗦的说明改写为更短的说法，删掉 4 条与上一行完全重复的提示

**内部（不影响使用）**

- 图标全部改为自绘，移除一个第三方图标库依赖，安装包更小
- 新增 5 条自动化测试守住上面这些视觉规范，避免以后的改动把它们改回去

## v0.7.2（2026-09-22）

**记加班弹窗与日期选择器打磨**

- 记加班弹窗半开⇄全开增加平滑的展开/收起动画，编辑态删除按钮随动画进出（不再瞬时跳变）
- 日期选择器精简：移除手动输入切换（同时修复切换时动画抽搐数秒的问题）与英文标题，收紧标题区留白
- 修复「保存」「取消」等短文案按钮文字未在按钮内居中的问题（工资页 / 记工页 / 记加班弹窗等）
- 存储源页：切换 WebDAV/S3 页签不再跳动；移除「当前使用」文字；断开按钮移至页底

## v0.7.1（2026-09-22）

**修复与打磨**

- 修复记加班弹窗分钟滚轮滚动时，中间数字左右被裁切的问题
- 修复记加班弹窗两个滚轮联动时的异常跳动与时长显示短暂不一致
- 修复日历农历宽字（如「初」「廿」「秋」）右侧被裁掉一小条的问题
- 记加班弹窗时长选择区新增凹陷底座，层级更清晰
- 底部导航栏投影调淡，整体更轻盈
- 分段选择控件（外观/统计等页）统一为凹陷轨道样式
- 同步备份页优化：未配置存储源时一键直达添加；自动备份并入云端卡片；
  本地导出/恢复按钮并排；存储源页切换页签不再跳动、断开按钮移至页底
- 「立即备份」「导出」等按钮统一为收缩 ✓ 反馈，与保存按钮一致
- 底部导航栏恢复原投影与按钮样式（前两项为本版内试验后的定稿回退）

## v0.7.0（2026-09-21）

**日历：节假日与农历大改版**

- 日历格重做为三段式：左侧小字标 **休**（放假）或 **班**（补班），**中间日期放大加粗成为格子主体**，右侧显示节日名或农历日期
- **农历日期正式上线**（此前日历只有节日标记、没有农历）：覆盖 1900–2100 年农历与二十四节气
- 节日名与农历都**紧贴日期**显示（此前贴在格子边缘，容易看成隔壁日期的信息）；两者同色同重，不抢日期的视觉主体
- 中秋、国庆这类连休，只在**第一天**显示节日名，中间几天显示农历（如「中秋 / 十六 / 十七」）；除夕单独标出
- 新增「外观 → 日历 → **隐藏农历日期**」开关（默认关闭）：打开后只显示节日名、不再显示农历
- ⚠️ **调休日加班费口径修正**：国务院调休连休日（如国庆连休里的 10/5）属于**休息日**，按**周末 2 倍**计算——此前误按法定节假日 3 倍。真正的法定节假日（每年 13 天）不变
- 同时补齐 2025 / 2026 的**调休连休日与补班日数据**（此前 2026 年缺全部补班日）：日历上的休/班标记、以及补班日的档位判定随之准确
- 数据源升级为分层结构：国务院调休单独一份、节日规则表单独一份，节假日数据可远程刷新且**升级 App 后自动采用内置的新数据**（不再被库里旧数据挡住）

**外观页改版**

- 原来一张卡塞 5 件事，拆成「深浅色 / 配色方案 / 弹层背景 / 日历」**四张卡**，一卡一事
- 卡片内外间距重新分级；深浅色与弹层背景改用**满宽等分的分段选择**（原来是大小不一的胶囊），三行控件的右边界对齐到卡片内容边缘
- 色卡：选中改为一圈主题色细环 + 圆内对勾（此前选中反而变小）；跟随壁纸取色时不再出现"一个都没选中"的空窗
- 配色方案 **4 套 → 8 套**：新增**朱砂**、**玫红**、**湖蓝**、**石墨**
- 配色方案改为**单行横向滑动**，今后再加配色也不会把卡片撑高
- 「暖橙」配色加深提彩度，白色文字在其上的对比度达到无障碍 AA 标准
- 修复外观页顶部一大块空白（页签切换时的垂直居中导致）
- 修复色卡点击涟漪是方块的问题；去掉标题行重复的「由壁纸决定」标签
- 首页卡片配置页去掉多余说明文字，页面更清爽

**底栏**

- 修复底栏毛玻璃在**「统计 → 明细」页签下失效**的问题（只有内容延伸到底栏后方的长列表页会触发）
- 底栏配置页：**已关闭的项目不再能被拖动排序**（拖拽手柄同时变淡）——开关只控制显示与否，顺序不会被误改
- 底栏配置页的预览**不再"点得动但没反应"**：预览是展示件，点击不再有按下缩放与涟漪

## v0.6.21（2026-09-20）

- **底栏毛玻璃**（外观 → 底栏「更多选项」里开启）：底栏半透明并**模糊身后的页面内容**，只糊背景、图标文字保持清晰；Android 12 以下自动回退为半透明。**出厂默认关闭**
- 底栏新增**固定长度**：开启后底栏固定为 4 个项目的长度，不随项目数自适应
- 底栏配置页改版：原来 4 张开关卡合并成一张**可折叠的「更多选项」卡**（点标题行展开/收起；高度、内容淡入淡出、箭头转向走同一条动画曲线）
- **底栏槽位与首页卡片的排序规则改版**：开关只控制**显示与否、不再改变顺序**，所有项目（含已关闭的）都能拖拽排序；「首页」「数据区」固定不可关闭
- 出厂默认调整：底栏默认**只有「首页 + 我的」**（其余项留在列表里、开关为关）；「更多选项」四个开关默认全关；首页默认隐藏「月柱状」「热点图」
- **固定顶栏新增「外观」入口**（齿轮左侧）：即使把「我的」从底栏移除，也能进入外观 / 首页 / 底栏 / 关于与设置
- 「按钮右置」的记加班圆钮：**与居中胶囊垂直居中对齐**，尺寸调整为与底栏文字按钮相称；开启毛玻璃时它也是同一套玻璃质感
- 底栏配置页的**预览与真实底栏完全一致**（含毛玻璃样式、宽度与位置；修正此前预览偏窄、已关闭的项也显示的问题）
- 同步备份可作为底栏页面：页签不再被固定顶栏遮住，横滑到尽头会自动切到相邻底栏页
- 修复：底栏/首页配置页的开关切换**没有过渡动画**；切换工时制度页的点击涟漪**超出卡片圆角**
- 切换工时制度的撤销窗口 **0.5 秒 → 1 秒**（倒计时结束自动回首页）
- 性能：修复底栏毛玻璃**空闲时每秒重绘 60 次**的问题，整体更顺、更省电
- 升级兼容：原有底栏 / 首页配置会**自动迁移保留**；从未配置过的按新的出厂默认

## v0.6.20（2026-09-18）

- 底栏配置更清爽：「记加班按钮置右」更名为**「按钮右置」**，并去掉其下方的说明文字
- **按钮右置**布局下的圆形记加班按钮缩小到与居中胶囊同尺寸（此前明显偏大）
- 底栏新增**悬浮质感**：加上投影与细描边，页面内容从底栏下方滚过时层级更清楚

## v0.6.19（2026-09-18）

- 修复：切换工时制度后**不会自动返回首页**的问题 —— 现在确认切换、0.5 秒撤销窗口一过即自动回到首页（撤销仍留在本页继续选）

## v0.6.18（2026-09-18）

- 弹层背景可自选：新增「外观 → 弹层背景」三档——压暗 / 模糊 / 模糊缩小（默认模糊），记加班弹窗与工地记工弹窗同步生效；旧版固定压暗
- 新增底栏布局：可在「外观 → 底栏」把记加班按钮改为底栏**右侧独立圆钮**（只显示图标），与原来的居中胶囊二选一
- 首页图表与统计页完全统一：本周柱状、月柱状、热力图都能**点击看浮窗、长按补记该日**，两页表现始终一致
- 修复图表浮窗首次弹出时只显示左半边、右半边随后才出现的问题
- 日历支持**长按后直接拖动连续多选**：一次手势划过的日期依次选中，批量记加班更快
- 底部悬浮提示位置上移，不再贴着屏幕下缘
- 记工页底部两颗按钮合并为**一颗「保存」**：原地保存并留在本页接着记（要离开点左上返回），保存时按钮收缩成对勾再展开；尺寸与记月页按钮一致
- 工资页「保存」、调休余额「增加调休 / 扣减」改用同款收缩对勾反馈；调休页未选时长时按钮变灰不可点
- 切换工时制度：撤销窗口缩短为 **0.5 秒**，倒计时一过**自动回首页**
- 统计页在底栏时：页签条下移到固定顶栏下方（不再被顶栏遮住）；横滑先切页签，滑到尽头继续滑会自动切到相邻底栏页
- 首页「统计」卡片、收入卡「明细」、工资页「查看记录」现在都进入**带返回的独立页**（不再跳到首页底栏的那个统计页）
- 出厂默认调整：底栏默认**仅图标**、首页默认包含**本周柱状卡**、弹层背景默认**模糊**
- 记月页「同步本月考勤」下方的说明文字已移除，页面更清爽

## v0.6.17（2026-09-17）

- 删除确认全部改为「原地展开」：点删除后按钮就地变成「取消 / 确认删除」，不再弹确认框、不再打断页面 —— 覆盖结算页删包工/借支、调休余额删调整记录、记工项目管理页删项目、项目编辑页删除/归档、班次删除
- 删除后可一键撤销：调休余额删调整记录、班次删除新增底部「撤销」（结算页删包工/借支原有的撤销保留），误删不怕；班次撤销连排序位置一起还原
- 记月页「导入上月」「同步本月考勤」改为原地确认 + 撤销：确认后按钮直接变「撤销」并带倒计时进度条；同步的覆盖范围说明改为按钮下方常驻小字（点之前就能看到，信息不丢）
- 切换工时制度不再弹窗：点哪张制度卡就在该卡下方原地展开「取消 / 确定切换」（带展开/收拢动画），确认后同样可倒计时撤销；撤销会把调休余额与转调休记录一并还原
- 班次管理更顺手：点行主体即改名（原「⋯」菜单取消），删除移到行内原地确认，行内顺序改为「删除 | 显示开关」
- 全 App 底部操作提示统一为浅色悬浮胶囊（成功打勾、失败警示），覆盖日历批量记加班、记月同步/导入、班次、项目管理、结算流水、调休余额
- 检查更新发现新版本时，按钮直接显示下载图标（更紧凑）

## v0.6.16（2026-09-17）

- 检查更新不再弹窗打断：点「检查更新」后原按钮直接变加载圆环，发现新版本时按钮变成「下载」，下载进度与「安装」都在原按钮上完成，全程无弹窗
- 底部操作提示升级为浅色悬浮胶囊：成功打勾、失败警示图标，不再是一条深色横条
- 导出 CSV、生成工资单、同步立即备份、导出备份文件时，按钮自身转圈提示进度，不再在按钮下方另起一行转圈
- 记加班弹窗点「保存」后平滑下滑关闭，和取消/返回一致，不再瞬间消失

## v0.6.15（2026-09-17）

- 结算页删除借支/包工记录不再弹确认框，删除后底部提示「已删除」，3 秒内可点「撤销」恢复，误删不怕
- 记工时滚轮选完时长会有轻微振动提示（定档感更清晰）
- 工时制度选择卡选中态更流畅：颜色与描边平滑过渡，选中带勾选角标
- 引导页步骤切换带方向滑动动画（前进右进、返回左出），切换更有方向感
- 开关类切换保持轻量无振动（避免频繁打扰），与工地表单「选择」语义区分

## v0.6.14（2026-09-16）

- 统计页图表可点化：点击周柱/月柱/热力图的任意一天，弹出该日日期与时长/工数浮窗（2 秒后自动消失）；长按可直接打开该日记工弹窗，补记更顺手
- 切换主题/配色时颜色平滑过渡，不再瞬变
- CSV 导入完成后提示可点「查看记录」，直达统计页明细查看导入结果
- 全 App 数字变化统一滚动动画：金额、时长、工时数等数字变化时沿增减方向滑动切换，不再生硬跳变
- 记加班/记工弹窗动效优化：遮罩改为原地淡入淡出（不再从下往上推），关闭时面板下滑淡出，不再突兀消失
- 调休余额手动调整时不再显示「将增加/扣减 xx 小时」预览提示，界面更清爽
- 结算页借支/计件记录的增删增加过渡动画：删除时行淡出消失、新增时淡入出现，金额联动更直观
- 首页今日工时胶囊数字切换带淡入上滑过渡
- 操作结果提示统一升级为底部悬浮提示（3 秒自动消失，不再挤压页面布局）：备份文件导出、调休余额调整、同步备份操作、节假日库刷新、CSV 导入导出
- 节假日库刷新时按钮显示转圈进度
- 更新下载转入后台后，可在「关于」页重新打开下载进度
- 记加班/请假保存增加振动反馈（与工资/工地表单同款反馈语言）
- 日历批量记加班完成后提示「已为 N 天记录加班」（部分失败显示成功天数）
- 记月页「同步本月考勤」「导入上月模板」完成后给出结果提示
- 结算页删除计件/借支前增加确认弹窗，确认后带振动反馈
- 长按头像重置增加振动反馈
- 补齐首页「记一笔」、工资/项目展开行的按压缩放反馈
- 修复结算页操作失败时错误提示不显示的问题：删除/撤销结算/部分结算失败现在会弹出明确提示
- 修复调休余额删除调整记录时无任何提示的问题：删除成功/失败现在都会给出明确反馈

---

## v0.6.13 (2026-09-15)

- 修复系统备份恢复后无法启动的问题（含换机迁移场景）；应用内原有的备份/恢复不受影响
- 加固数据安全：备份包增加完整性校验，自签证书改用指纹校验（防中间人），备份文件损坏时不再崩溃
- 修复工地记工结算相关的几处记账问题：已结算的记录不能再改、借支与包工不能再记到已结算的日期
- 关于页新增「致谢」名单卡：渐变色玻璃卡片，带上边流动彩虹与星光装饰，可左右滑动查看
- 工资设置页保存后按钮显示 ✓ 提示，明确告知保存成功
- 明细页改为最新记录在前

---

## v0.6.12 (2026-09-14)

- 「关于与隐私」页升级为「关于 / 设置」双页签（原设置中心并入「设置」页签）；「我的」页入口更名为「关于与设置」
- 首页顶栏切换工时按钮改为直达「切换工时制度」页；右上角齿轮进入按当前工时制度的设定多页签页（支持横滑切换）：
  标准工时=工资 / 调休 / 周期 / 工作日 / 班次，综合工时=工资 / 周期 / 工作日 / 班次，小时工=工资 / 周期 / 班次，工地记工=项目 / 结算
- 原「工时设置」入口列表页与工地的「项目与结算」入口页移除，显示单位切换按钮移除
- 首页收入卡「明细」直达统计页「明细」页签，独立明细页移除

---

## v0.6.11 (2026-09-14)

- 统计页升级为「统计 / 记月 / 明细」三页签：明细（本周期收入构成）并入统计页，横滑即可查看（工地记工模式下为「统计 / 明细」两页签）；首页收入卡进入的明细页保持不变
- 「记月」页签调整为仅标准工时与综合工时显示（小时工、工地记工模式下为「统计 / 明细」两页签）
- 日历页今天之后的日期置灰且不可选中，也不能再切换到未来月份
- 「存储源」页 WebDAV / S3 子页签支持左右滑动切换，滑块随手势连续跟随；并修复子页签上方可能出现的大片空白
- 存储源子页签滑动到尽头继续滑，可自然切换到「备份」页签
- 备份页「备份与恢复」卡精简重复提示文案

---

## v0.6.10 (2026-09-13)

- 「备份 / 存储源」与「外观 / 首页 / 底栏」页签支持左右滑动切换，与统计页「统计 / 记月」操作方式一致

---

## v0.6.9 (2026-09-13)

- 「同步备份」与「存储源」合并为「备份 / 存储源」双页签，顶栏名为「备份」；备份状态直接显示在「备份与恢复」卡内，存储源的 WebDAV / S3 改为子页签切换
- 「外观」并入配置页，现为「外观 / 首页 / 底栏」三页签（从「外观」入口进入默认落外观页签）；底栏配置去掉「功能」小标题
- 修复工地记工「包工 / 借支 / 结算」三页卡片贴边（与备注照片卡之间补足间距）
- 修复同步备份页偶发闪退（页签切换改为稳定实现）

---

## v0.6.8 (2026-09-13)

- 全部二级页顶栏统一为胶囊标题（与页签切换同款视觉，为后续页签化拓展预留）
- 「首页卡片」与「底栏配置」合并为「首页 / 底栏」双页签页：外观页两个入口保留，进入同页仅默认页签不同
- 首页卡片配置合并为单卡总表：显示中（可拖拽排序）在前、已隐藏在后，开关即归组
- 工地记工记录页手势分区：内容区横滑切子页签（点工/包工、借支/结算），子页签尽头继续滑联动上级（包工→借支）；顶栏横滑切 记账↔记借支/结算
- 记月「同步本月考勤」修复为一次生效；补贴/扣款添加对话框新增预选项快选（高温/夜班/餐补/住房/交通/全勤奖、水电/房租/伙食费等）

---

## v0.6.7 (2026-09-13)

- 页签切换全面统一为滑块式：工地记工记录页顶栏与 点工/包工、借支/结算 子页签换新，统计页顶栏分段控件样式同步对齐
- 工地记工模式下统计页隐藏「记月」页签（该制度后续配专属页面）

---

## v0.6.6 (2026-09-12)

- 统计页顶栏升级：统计/记月双页签，选中块跟随滑动、内容区横滑同步
- 全新「记月」页：月度工资单——基本/补贴/扣款/其他四分组，折叠展开、金额编辑、添加/删除条目、月份切换与一键导入上月
- 「同步本月考勤」：按当月加班/请假记录一键回填 加班工资、事假、病假（已设底薪含基本工资），回填后仍可手动微调
- 「我的」页数据卡按工时制度切换：工地记工展示 本年工天/完工项目/本年工钱

---

## v0.6.5 (2026-09-12)

- 「关于」页全新版本卡：版本大字 + 检查更新，花体 from 一键查看当前更新源
- 更新数据源弹窗化管理：名称 + 地址一目了然，点选即切换，可添加/删除地址（内置 CNB 与 GitHub 双源）
- 「更新与数据源」独立页取消，入口收进关于页；节假日库仍随启动自动保持最新

---

## v0.6.4 (2026-09-12)

- 「更新与数据源」页焕新：版本大字卡片，可检查更新
- 更新与节假日 JSON 地址支持多选一：内置 CNB（默认）与 GitHub Pages 双候选，可自行添加/删除/切换，源故障一键备用
- 默认 JSON 源切换为 CNB（国内可达性优先）

---

## v0.6.3 (2026-09-12)

- 设置中心重新开放「更新与数据源」页：查看当前版本、检查更新、自定义更新 JSON 地址与节假日库地址（为 GitHub Pages 与 CNB 双更新源提供切换入口）

---

## v0.6.2 (2026-09-11)

- 新增首页「月柱状」卡片（默认关闭，设置 → 外观 → 首页卡片开启）：整月每日加班分布柱状图，与统计页同款
- 热点图升级：固定展示最近六个月（本月向前推五个月），首页与统计页一致
- 明细页重设计：每条明细一行——「日期 · 班次 · 档位徽章」+ 右侧时长与金额上下对照，页面更清爽
- 统计页与明细页的日期范围统一为日期胶囊样式；汇总卡三档金额带上 ¥ 符号
- 明细行点击新增弹性按压动效
- 修复：统计页三档收入胶囊被挤成两行、金额显示不全的问题

---

## v0.6.1 (2026-09-11)

- 修复：从 v0.5.10 直接升级后启动闪退（数据库升级顺序问题，已在真机复现并验证修复）

---

## v0.6.0 (2026-09-11)

- 新增「工地记工」工时制度：按项目记工天/加班/借支，待结余额自动计算，支持多次部分结算与整期结清、结算单快照归档
- 新增「明细」页：首页收入卡点击进入，逐条查看本周期收入构成（加班档位、工地流水）
- 统计页升级：新增月柱状图（整月每日分布 + 最多日提示），汇总卡按制度呈现三色钱账/档位分布
- 首页数据区美化：工时大字带单位、今日速览胶囊（没记提示可点击去记）、项目色点、收入卡升级高亮样式
- 同步备份收敛：网盘与本地各只保留一个压缩包，目录更清爽
- 日历页在工地记工模式下点击记工按钮直达记工页面
- 修复：记借支后进入结算页闪退；结算周期显示为 1 月 1 日的问题；删除最后一个项目后的兜底处理

---

## v0.5.10 (2026-09-08)

- 修复 Android 15+ 设备上应用内自更新无法调起安装器的问题
- 修复更新包下载中断后，残留损坏缓存被重复使用导致安装失败的问题

## v0.5.9 (2026-09-08)

- 适配平板与宽屏：两档断点（600/840dp），内容限宽居中，不再拉伸铺满
- 平板横屏下首页、日历、统计、设置自动启用双栏布局，信息密度更高
- 宽屏下记加班弹窗改为居中悬浮面板形态
- 动效全面改造：底栏、日历、统计图表、时长选择等动画升级为弹性动效，更自然流畅
---

## v0.5.8 (2026-09-08)

- 全面升级 Material 3 Expressive 设计主题：全新动效、圆角与交互反馈
- 输入框统一为浮动标签样式：聚焦或输入后标签自动浮起，更清晰规范
- 记加班弹窗备注框样式统一
- 班次改名弹窗简化美化

## v0.5.7 (2026-09-07)

- 修复应用内更新时提示「没有权限安装」的问题：安装方式改用 PackageInstaller Session API
- 无需「安装未知应用」权限，系统直接弹出安装确认界面

## v0.5.6 (2026-09-07)

- 图标全面改版为 Material Symbols Rounded 单色风格，更统一更现代
- 图标底色统一为主题色容器（青绿/蓝/橙/紫），不再是灰色
- 修复「外观」设置行副文本显示数字（资源 id）的问题
- 桌面图标新增 monochrome 层，支持 Android 主题图标

## v0.5.5 (2026-09-07)

- 更新下载进度弹窗新增「后台下载」按钮：下载中可收起弹窗继续使用 App，完成后提示安装
- 移除安装权限申请，直接调起系统安装器（后于 v0.5.7 改为 Session API 彻底解决）

## v0.5.4 (2026-09-07)

- 启动图标换新为招财兔
- 存储源支持多源配置：同一种存储方式（如 WebDAV）可添加多个账号/服务器
- 存储源页面改为卡片式布局，支持编辑配置
- 切换存储源时先检测连接可用性，不可用则提示
- S3 存储源入口暂时隐藏（待测试）

## v0.5.3 (2026-09-07)

- 项目开源（GitHub: 3546475/mdot），Release 由 GitHub Actions 自动签名构建
- 新增应用内自更新：关于页「检查更新」→ 下载 → 安装全链路，无需跳浏览器
- 更新信息托管于 GitHub Pages，支持自定义更新源
